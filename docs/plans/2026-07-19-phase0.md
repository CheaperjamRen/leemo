# Leemo 第一批实施计划：仓库初始化 + Phase 0 可行性验证

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立 `E:\Leemo` 新仓库，并用可复现的 smoke 脚本证明 `@anthropic-ai/claude-agent-sdk@0.3.210` 在 DeepSeek/GLM/Kimi 三个国产 Anthropic 兼容端点上通过五项核心验证（流式/工具调用/多轮/子 agent/上下文压缩）+ 四项探测，产出 Phase 0 报告与 PASS 判定。

**Architecture:** 纯 Node ESM 脚本仓库（本批不引入 Electron/前端）。`smoke/` 下按职责分四个文件：providers 目录表、共享 lib（env 构造/消息收集）、五项核心验证、四项探测；一个 CLI runner 逐项执行并把结构化结果落 JSON，报告由 JSON 汇编。每项验证在独立临时工作区目录运行，互不污染。

**Tech Stack:** Node.js ≥18（推荐 20+）、`@anthropic-ai/claude-agent-sdk@0.3.210`（锁死精确版本）、零其它运行时依赖（.env 手写解析，不装 dotenv）。

## Global Constraints（每张任务卡隐含遵守）

- SDK 版本 **精确锁定 0.3.210**（`--save-exact`），任何步骤不得升级/改版本。
- 新代码只进 `E:\Leemo\`；**禁改清单（全局）：`E:\幸运鹿AI\` 下任何文件一律只读**。
- API Key 只经 `E:\Leemo\.env`（已 gitignore）传入；**任何文件、commit、日志输出不得含明文 key**（runner 打印时脱敏）。
- 命名：产品=Leemo，人格=momo；代码/文档不得出现"幸运鹿/LuckyDeer/Lulu"字样。
- 执行环境 = 用户 Windows 10 机器；验收命令用 PowerShell 5.1 语法（**没有 `&&`**，用 `;` 或分行）。
- 执行者≠验收者：每张卡产出可复现证据（命令+输出），验收方复跑核对。
- 设计文档权威：`docs/specs/06-Leemo-产品设计文档-v1.0.md`（Task 1 拷入仓库）。

**关于 TDD 边界的说明**：本批全部是"验证脚本"性质（smoke/探测），不适用严格 TDD 单元测试循环；每步以"语法自检 → 真端点试跑 → 结构化证据"替代红绿循环。Bridge/网关等严格 TDD 从下一批开始。

**已核实的 SDK API 事实（写代码时直接依赖，不必再查）**：

- `import { query } from '@anthropic-ai/claude-agent-sdk'`；`query({ prompt, options })` 返回 AsyncGenerator。`prompt` 可为 string 或 `AsyncIterable<SDKUserMessage>`（多轮）。
- `options.env` **整体替换**子进程环境（不与 process.env 合并），必须自己 spread `...process.env`。
- 关键 options：`cwd`、`maxTurns`、`permissionMode`（'default'|'acceptEdits'|...）、`includePartialMessages`（true 才有 `stream_event` 消息）、`canUseTool(toolName, input, {signal,...}) => {behavior:'allow',updatedInput}|{behavior:'deny',message}`、`resume`（session id 续接）、`settingSources: []`（隔离用户机器上的 ~/.claude 设置，必须显式传空数组）。
- 消息类型（`for await (const m of q)`）：`m.type === 'assistant'`（`m.message.content` 是 block 数组，`b.type==='tool_use'` 有 `b.name`；子 agent 产生的消息带 `m.parent_tool_use_id`）；`'stream_event'`（流式增量）；`'result'`（`subtype: 'success'` 时有 `m.result` 文本、`usage`、`total_cost_usd`、`num_turns`；error 时 `subtype` 为 `error_during_execution` 等且有 `errors` 数组）；`'system'` + `subtype:'init'`（有 `session_id`、`model`、`tools`）；`'system'` + `subtype:'compact_boundary'`（有 `compact_metadata.trigger/pre_tokens/post_tokens`）。
- SDKUserMessage 形状：`{ type:'user', message:{ role:'user', content:'...' }, parent_tool_use_id: null, session_id: '' }`。
- 流式输入模式下先后 yield 的多条 user 消息按队列逐轮处理（这是多轮验证的机制基础）。
- claude.exe 由 optionalDependency `@anthropic-ai/claude-agent-sdk-win32-x64` 自动安装，SDK 自动找到，无需 `pathToClaudeCodeExecutable`。

---

### Task 1: 仓库初始化（E:\Leemo + GitHub 私有仓）

**Files:**
- Create: `E:\Leemo\.gitignore`
- Create: `E:\Leemo\package.json`（npm init 生成后修改）
- Create: `E:\Leemo\README.md`
- Create: `E:\Leemo\docs\specs\06-Leemo-产品设计文档-v1.0.md`（从旧工作区**复制**）
- Create: `E:\Leemo\docs\specs\02-已定决策清单.md`（复制）
- Create: `E:\Leemo\docs\plans\2026-07-19-phase0.md`（本计划文件复制）
- Create: `E:\Leemo\.env.example`

**Interfaces:**
- Produces: 可用的 git 仓库 + `node_modules/@anthropic-ai/claude-agent-sdk`（0.3.210）供 Task 2-5 import；`.env.example` 定义的变量名（`DEEPSEEK_API_KEY`/`DEEPSEEK_MODEL`/`GLM_API_KEY`/`GLM_MODEL`/`KIMI_API_KEY`/`KIMI_MODEL`/`RELAY_BASE_URL`/`RELAY_API_KEY`/`RELAY_MODEL`）是后续所有任务读 key 的唯一约定。

**禁改清单：** `E:\幸运鹿AI\` 只读（复制源文件不动原件）。

- [ ] **Step 1: 建目录并 git init**

```powershell
New-Item -ItemType Directory -Force E:\Leemo | Out-Null
Set-Location E:\Leemo
git init -b main
git config user.name  # 若为空，按用户 GitHub 账号配置 user.name/user.email
```

- [ ] **Step 2: 写 .gitignore**

创建 `E:\Leemo\.gitignore`：

```gitignore
node_modules/
.env
smoke/results/
smoke/workspaces/
dist/
*.log
```

- [ ] **Step 3: 写 package.json**

```powershell
Set-Location E:\Leemo; npm init -y | Out-Null
```

然后把 `E:\Leemo\package.json` 整体替换为：

```json
{
  "name": "leemo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Leemo — AI companion & workbench built on Claude Agent SDK",
  "engines": { "node": ">=18.0.0" },
  "scripts": {
    "smoke": "node smoke/smoke-cc-sdk.mjs"
  }
}
```

- [ ] **Step 4: 安装 SDK（精确锁版）**

```powershell
Set-Location E:\Leemo; npm install --save-exact @anthropic-ai/claude-agent-sdk@0.3.210
```

预期：安装成功；`package.json` 依赖为 `"@anthropic-ai/claude-agent-sdk": "0.3.210"`（无 `^`）。Windows 上会自动带入 `@anthropic-ai/claude-agent-sdk-win32-x64`（~253MB，下载慢属正常）。

- [ ] **Step 5: 验证 SDK 与二进制就位**

```powershell
Set-Location E:\Leemo
npm ls @anthropic-ai/claude-agent-sdk
node -e "import('@anthropic-ai/claude-agent-sdk').then(m => console.log('query is', typeof m.query))"
Test-Path node_modules\@anthropic-ai\claude-agent-sdk-win32-x64
```

预期：版本行显示 `0.3.210`；输出 `query is function`；`True`。

- [ ] **Step 6: 拷贝权威文档 + 写 README + .env.example**

```powershell
New-Item -ItemType Directory -Force E:\Leemo\docs\specs, E:\Leemo\docs\plans, E:\Leemo\docs\reports | Out-Null
Copy-Item "E:\幸运鹿AI\竞品调研与反思\2026-07-16_CC-SDK引擎决策与产品规划\06-Leemo-产品设计文档-v1.0.md" E:\Leemo\docs\specs\
Copy-Item "E:\幸运鹿AI\竞品调研与反思\2026-07-16_CC-SDK引擎决策与产品规划\handoff-superpowers\02-已定决策清单.md" E:\Leemo\docs\specs\
Copy-Item "E:\幸运鹿AI\竞品调研与反思\2026-07-16_CC-SDK引擎决策与产品规划\07-Phase0-实施计划.md" E:\Leemo\docs\plans\2026-07-19-phase0.md
```

创建 `E:\Leemo\README.md`：

```markdown
# Leemo

AI companion & workbench built on Claude Agent SDK（人格：momo）。

- 设计文档：`docs/specs/06-Leemo-产品设计文档-v1.0.md`（权威）
- 决策宪法：`docs/specs/02-已定决策清单.md`
- 当前阶段：Phase 0 可行性验证（`docs/plans/2026-07-19-phase0.md`）

## Phase 0 smoke

1. `copy .env.example .env`，填入各家 API Key（.env 已 gitignore，永不提交）
2. `npm run smoke -- --provider deepseek --check all`
3. 报告见 `docs/reports/phase0-report.md`
```

创建 `E:\Leemo\.env.example`：

```ini
# 各家模型名以其官方 Claude Code 接入文档为准；填错会报 model_not_found
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-chat
GLM_API_KEY=
GLM_MODEL=glm-5.2
KIMI_API_KEY=
KIMI_MODEL=kimi-k2.5
# 探测项 P3：用户中转站（可留空跳过）
RELAY_BASE_URL=
RELAY_API_KEY=
RELAY_MODEL=
```

- [ ] **Step 7: 首次提交**

```powershell
Set-Location E:\Leemo
git add -A
git commit -m "chore: init Leemo repo, lock claude-agent-sdk@0.3.210, import specs"
git log --oneline
```

预期：1 个 commit；`git status` 干净且 node_modules/.env 未入库。

- [ ] **Step 8: 建 GitHub 私有仓并推送**

优先用 gh CLI：

```powershell
gh --version
gh repo create leemo --private --source E:\Leemo --push
```

若无 gh：让用户在 github.com 手动创建私有仓 `leemo`（不初始化 README），然后：

```powershell
Set-Location E:\Leemo
git remote add origin https://github.com/<用户名>/leemo.git
git push -u origin main
```

预期：`git remote -v` 显示 origin；push 成功。

**验收命令（验收方复跑）：**

```powershell
Set-Location E:\Leemo; git log --oneline; npm ls @anthropic-ai/claude-agent-sdk; git remote -v; git status --short
```

预期：commit 存在、版本 0.3.210、origin 指向私有仓、status 无未跟踪的敏感文件。

---

### Task 2: smoke 基础设施（env 加载 / Provider 目录 / runner 骨架）

**Files:**
- Create: `E:\Leemo\smoke\lib.mjs`
- Create: `E:\Leemo\smoke\providers.mjs`
- Create: `E:\Leemo\smoke\smoke-cc-sdk.mjs`

**Interfaces:**
- Consumes: Task 1 的 `.env` 变量约定与已装 SDK。
- Produces: `lib.mjs` 导出 `loadEnv()`, `buildEnv(provider)`, `u(content)`, `newWorkspace(tag)`, `redact(s)`；`providers.mjs` 导出 `PROVIDERS`, `resolveProvider(id)`；runner 支持 `--provider <id|all> --check <name,...|all|none> --probes`，结果写 `smoke/results/<provider>-<timestamp>.json`。Task 3/4 只需向 `CHECKS`/`PROBES` 注册表添加函数。

**禁改清单：** 不改 Task 1 产物（package.json 除 scripts 外不动）；不新增 npm 依赖。

- [ ] **Step 1: 写 `smoke/lib.mjs`**

```js
// smoke/lib.mjs — 共享工具：env 加载、SDK 环境构造、工作区、脱敏
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');

export function loadEnv() {
  const envPath = path.join(ROOT, '.env');
  if (!fs.existsSync(envPath)) return;
  for (const line of fs.readFileSync(envPath, 'utf8').split(/\r?\n/)) {
    const m = line.match(/^\s*([A-Z0-9_]+)\s*=\s*(.*)\s*$/);
    if (m && !line.trim().startsWith('#') && m[2] !== '') process.env[m[1]] ??= m[2];
  }
}

// options.env 整体替换子进程环境，必须 spread process.env
export function buildEnv(provider) {
  return {
    ...process.env,
    ANTHROPIC_BASE_URL: provider.baseUrl,
    ANTHROPIC_AUTH_TOKEN: provider.apiKey,
    ANTHROPIC_API_KEY: '',
    ANTHROPIC_MODEL: provider.model,
    ANTHROPIC_DEFAULT_SONNET_MODEL: provider.model,
    ANTHROPIC_DEFAULT_OPUS_MODEL: provider.model,
    ANTHROPIC_DEFAULT_HAIKU_MODEL: provider.model,
    ANTHROPIC_SMALL_FAST_MODEL: provider.model,
    CLAUDE_CODE_SUBAGENT_MODEL: provider.model,
    CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC: '1',
  };
}

// 流式输入模式的 user 消息形状
export const u = (content) => ({
  type: 'user',
  message: { role: 'user', content },
  parent_tool_use_id: null,
  session_id: '',
});

export function newWorkspace(tag) {
  const dir = path.join(ROOT, 'smoke', 'workspaces', `${tag}-${Date.now()}`);
  fs.mkdirSync(dir, { recursive: true });
  return dir;
}

export function redact(str) {
  let out = String(str);
  for (const [k, v] of Object.entries(process.env)) {
    if (/(_API_KEY|_TOKEN)$/.test(k) && v && v.length > 8) out = out.split(v).join(`<${k}>`);
  }
  return out;
}

export function saveResult(providerId, data) {
  const dir = path.join(ROOT, 'smoke', 'results');
  fs.mkdirSync(dir, { recursive: true });
  const file = path.join(dir, `${providerId}-${new Date().toISOString().replace(/[:.]/g, '-')}.json`);
  fs.writeFileSync(file, redact(JSON.stringify(data, null, 2)));
  return file;
}
```

- [ ] **Step 2: 写 `smoke/providers.mjs`**

```js
// smoke/providers.mjs — Provider 目录（06 号设计文档 §3.1 的最小前身）
export const PROVIDERS = {
  deepseek: { name: 'DeepSeek', baseUrl: 'https://api.deepseek.com/anthropic', keyEnv: 'DEEPSEEK_API_KEY', modelEnv: 'DEEPSEEK_MODEL', defaultModel: 'deepseek-chat' },
  glm:      { name: 'GLM',      baseUrl: 'https://open.bigmodel.cn/api/anthropic', keyEnv: 'GLM_API_KEY', modelEnv: 'GLM_MODEL', defaultModel: 'glm-5.2' },
  kimi:     { name: 'Kimi',     baseUrl: 'https://api.moonshot.cn/anthropic', keyEnv: 'KIMI_API_KEY', modelEnv: 'KIMI_MODEL', defaultModel: 'kimi-k2.5' },
};

export function resolveProvider(id) {
  const p = PROVIDERS[id];
  if (!p) throw new Error(`未知 provider: ${id}（可选: ${Object.keys(PROVIDERS).join('/')}）`);
  const apiKey = process.env[p.keyEnv];
  if (!apiKey) throw new Error(`${p.keyEnv} 未配置——在 E:\\Leemo\\.env 里填入后重试`);
  return { id, ...p, apiKey, model: process.env[p.modelEnv] || p.defaultModel };
}
```

- [ ] **Step 3: 写 `smoke/smoke-cc-sdk.mjs`（runner 骨架，CHECKS/PROBES 注册表先留空）**

```js
// smoke/smoke-cc-sdk.mjs — Phase 0 runner
// 用法: node smoke/smoke-cc-sdk.mjs --provider deepseek|glm|kimi|all --check all|streaming,tools,... --probes
import { loadEnv, saveResult } from './lib.mjs';
import { PROVIDERS, resolveProvider } from './providers.mjs';

loadEnv();

// Task 3 在此注册五项核心验证；Task 4 注册探测
export const CHECKS = {};   // { streaming: fn(provider), tools: fn, multiturn: fn, subagent: fn, compaction: fn }
export const PROBES = {};   // { resume: fn(provider), anysearch: fn, 'relay-anthropic': fn, canusetool: fn(provider) }

function arg(name, dflt) {
  const i = process.argv.indexOf(`--${name}`);
  if (i === -1) return dflt;
  const v = process.argv[i + 1];
  return v && !v.startsWith('--') ? v : true;
}

const providerArg = arg('provider', 'deepseek');
const checkArg = arg('check', 'all');
const runProbes = process.argv.includes('--probes');

const providerIds = providerArg === 'all' ? Object.keys(PROVIDERS) : [providerArg];
const checkNames = checkArg === 'none' ? [] : checkArg === 'all' ? Object.keys(CHECKS) : String(checkArg).split(',');

for (const pid of providerIds) {
  let provider;
  try { provider = resolveProvider(pid); }
  catch (e) { console.error(`[skip] ${pid}: ${e.message}`); continue; }

  console.log(`\n===== ${provider.name} (${provider.model}) =====`);
  const run = { provider: pid, model: provider.model, startedAt: new Date().toISOString(), checks: [], probes: [] };

  for (const name of checkNames) {
    if (!CHECKS[name]) { console.error(`[skip] 未知 check: ${name}`); continue; }
    console.log(`\n--- check: ${name} ---`);
    const t0 = Date.now();
    try {
      const r = await CHECKS[name](provider);
      r.ms = Date.now() - t0;
      run.checks.push(r);
      console.log(`${r.pass ? 'PASS' : 'FAIL'} (${r.ms}ms)`, JSON.stringify(r.details).slice(0, 300));
    } catch (e) {
      run.checks.push({ check: name, pass: false, error: String(e), ms: Date.now() - t0 });
      console.log(`ERROR: ${e}`);
    }
  }

  if (runProbes) {
    for (const [name, fn] of Object.entries(PROBES)) {
      console.log(`\n--- probe: ${name} ---`);
      try {
        const r = await fn(provider);
        run.probes.push(r);
        console.log(`ok=${r.ok}`, JSON.stringify(r.details).slice(0, 300));
      } catch (e) { run.probes.push({ probe: name, ok: false, error: String(e) }); console.log(`ERROR: ${e}`); }
    }
  }

  const file = saveResult(pid, run);
  const passed = run.checks.filter(c => c.pass).length;
  console.log(`\n${provider.name}: ${passed}/${run.checks.length} checks pass → ${file}`);
}
```

- [ ] **Step 4: 语法与骨架自检（无 key 也能跑）**

```powershell
Set-Location E:\Leemo
node --check smoke\lib.mjs; node --check smoke\providers.mjs; node --check smoke\smoke-cc-sdk.mjs
node smoke\smoke-cc-sdk.mjs --provider glm --check none
node smoke\smoke-cc-sdk.mjs --provider nosuch --check none
```

预期：三个 `--check` 无输出（语法通过）；glm 无 key 时打印含 `GLM_API_KEY 未配置` 的 `[skip]`（或有 key 时打印 `===== GLM =====` 与 0 checks）；nosuch 打印 `未知 provider`。均不崩溃、退出码 0。

- [ ] **Step 5: 提交**

```powershell
Set-Location E:\Leemo; git add smoke; git commit -m "feat(smoke): runner skeleton with provider catalog and env loading"
```

**验收命令：** 同 Step 4，外加 `git log --oneline` 见新 commit；`Get-Content smoke\providers.mjs` 核对三端点 URL 与 06 号文档 §3.1 一致。

---

### Task 3: 五项核心验证实现 + DeepSeek 首跑

**Files:**
- Create: `E:\Leemo\smoke\checks.mjs`
- Modify: `E:\Leemo\smoke\smoke-cc-sdk.mjs`（只改 CHECKS 注册处与 import）

**Interfaces:**
- Consumes: `lib.mjs` 的 `buildEnv/u/newWorkspace`；`resolveProvider` 返回的 `{baseUrl, apiKey, model}`。
- Produces: `checks.mjs` 导出 `checkStreaming/checkTools/checkMultiTurn/checkSubagent/checkCompaction`，每个签名 `async (provider) => ({ check, pass, details })`。

**禁改清单：** 不改 `lib.mjs`/`providers.mjs`；runner 只允许加 import 和填 CHECKS。

- [ ] **Step 1: 写 `smoke/checks.mjs`**

```js
// smoke/checks.mjs — Phase 0 五项核心验证
import fs from 'node:fs/promises';
import path from 'node:path';
import { query } from '@anthropic-ai/claude-agent-sdk';
import { buildEnv, u, newWorkspace } from './lib.mjs';

function baseOptions(provider, cwd, extra = {}) {
  return {
    cwd,
    env: buildEnv(provider),
    permissionMode: 'acceptEdits',
    settingSources: [],          // 隔离本机 ~/.claude 设置，保证可复现
    maxTurns: 10,
    ...extra,
  };
}

// ① 流式：stream_event 增量 ≥5 且正常收束
export async function checkStreaming(provider) {
  const cwd = newWorkspace(`${provider.id}-streaming`);
  const q = query({
    prompt: '请从 1 数到 5，每个数字单独一行，不要多余内容。',
    options: baseOptions(provider, cwd, { includePartialMessages: true, maxTurns: 3 }),
  });
  let streamEvents = 0, result = null;
  for await (const m of q) {
    if (m.type === 'stream_event') streamEvents++;
    if (m.type === 'result') result = m;
  }
  return {
    check: 'streaming',
    pass: streamEvents >= 5 && result?.subtype === 'success' && !result.is_error,
    details: { streamEvents, subtype: result?.subtype, usage: result?.usage },
  };
}

// ② 内置工具：真实读文件 + 写文件
export async function checkTools(provider) {
  const cwd = newWorkspace(`${provider.id}-tools`);
  await fs.writeFile(path.join(cwd, 'notes.md'), '# 会议纪要\n- Leemo 定于 2026 年秋季发布\n- 默认推荐 DeepSeek\n');
  const q = query({
    prompt: '读取 notes.md，把其中提到的发布时间写入 answer.txt（只写时间本身，不要别的字）。',
    options: baseOptions(provider, cwd),
  });
  const toolsUsed = [];
  let result = null;
  for await (const m of q) {
    if (m.type === 'assistant') for (const b of m.message.content) { if (b.type === 'tool_use') toolsUsed.push(b.name); }
    if (m.type === 'result') result = m;
  }
  let content = '';
  try { content = await fs.readFile(path.join(cwd, 'answer.txt'), 'utf8'); } catch {}
  return {
    check: 'tools',
    pass: toolsUsed.length > 0 && content.includes('2026') && result?.subtype === 'success',
    details: { toolsUsed, answerContent: content.trim().slice(0, 80) },
  };
}

// ③ 多轮：同 session 内第二轮引用第一轮信息（流式输入队列，两条依次成轮）
export async function checkMultiTurn(provider) {
  const cwd = newWorkspace(`${provider.id}-multiturn`);
  async function* turns() {
    yield u('请记住这个暗号：蓝色鲸鱼 42。记住后只回复"记住了"。');
    yield u('我刚才让你记住的暗号是什么？一字不差地说出来。');
  }
  const q = query({ prompt: turns(), options: baseOptions(provider, cwd, { maxTurns: 6 }) });
  let assistantTurns = 0, lastResult = null;
  for await (const m of q) {
    if (m.type === 'assistant' && !m.parent_tool_use_id) assistantTurns++;
    if (m.type === 'result') lastResult = m;
  }
  const finalText = lastResult?.subtype === 'success' ? lastResult.result || '' : '';
  return {
    check: 'multiturn',
    pass: /42/.test(finalText) && lastResult?.subtype === 'success',
    details: { assistantTurns, finalText: finalText.slice(0, 120) },
  };
}

// ④ 子 agent：强制走 Task 工具，验证派生与回收
export async function checkSubagent(provider) {
  const cwd = newWorkspace(`${provider.id}-subagent`);
  for (const [i, txt] of ['alpha', 'beta', 'gamma'].entries()) {
    await fs.writeFile(path.join(cwd, `f${i + 1}.txt`), txt + '\n');
  }
  const q = query({
    prompt: '使用 Task 工具派一个子 agent 统计当前目录下 .txt 文件的数量和文件名，然后向我汇报。必须用 Task 工具，不要自己直接数。',
    options: baseOptions(provider, cwd, { maxTurns: 15 }),
  });
  let taskToolUsed = false, subagentActivity = 0, result = null;
  for await (const m of q) {
    if (m.type === 'assistant') {
      if (m.parent_tool_use_id) subagentActivity++;
      for (const b of m.message.content) { if (b.type === 'tool_use' && b.name === 'Task') taskToolUsed = true; }
    }
    if (m.type === 'user' && m.parent_tool_use_id) subagentActivity++;
    if (m.type === 'result') result = m;
  }
  const answer = result?.subtype === 'success' ? result.result || '' : '';
  return {
    check: 'subagent',
    pass: taskToolUsed && /3|三/.test(answer) && result?.subtype === 'success',
    details: { taskToolUsed, subagentActivity, answer: answer.slice(0, 150) },
  };
}

// ⑤ 上下文压缩：/compact 手动触发（走 provider 的摘要请求，验证压缩管线；
//    自动触发需灌 10 万+ tokens，不在 smoke 范围，报告里注明）
export async function checkCompaction(provider) {
  const cwd = newWorkspace(`${provider.id}-compaction`);
  async function* turns() {
    yield u('请完整复述并记住这个设定：Leemo 是基于 Claude Agent SDK 的桌面 AI 工作台，人格叫 momo，暗号是紫色大象 88。');
    yield u('/compact');
    yield u('压缩之后问你：设定里的暗号是什么？直接说出。');
  }
  const q = query({ prompt: turns(), options: baseOptions(provider, cwd) });
  let boundary = null, lastResult = null;
  for await (const m of q) {
    if (m.type === 'system' && m.subtype === 'compact_boundary') boundary = m.compact_metadata;
    if (m.type === 'result') lastResult = m;
  }
  const finalText = lastResult?.subtype === 'success' ? lastResult.result || '' : '';
  return {
    check: 'compaction',
    pass: Boolean(boundary) && /88/.test(finalText),
    details: { trigger: boundary?.trigger, pre_tokens: boundary?.pre_tokens, post_tokens: boundary?.post_tokens, finalText: finalText.slice(0, 120) },
  };
}
```

- [ ] **Step 2: 在 runner 注册**

`smoke/smoke-cc-sdk.mjs` 顶部加 import，并把 `export const CHECKS = {};` 替换为：

```js
import { checkStreaming, checkTools, checkMultiTurn, checkSubagent, checkCompaction } from './checks.mjs';

export const CHECKS = {
  streaming: checkStreaming,
  tools: checkTools,
  multiturn: checkMultiTurn,
  subagent: checkSubagent,
  compaction: checkCompaction,
};
```

- [ ] **Step 3: 语法自检**

```powershell
Set-Location E:\Leemo; node --check smoke\checks.mjs; node --check smoke\smoke-cc-sdk.mjs
```

预期：无输出。

- [ ] **Step 4: DeepSeek 单项试跑（需 .env 已配 DEEPSEEK_API_KEY）**

```powershell
Set-Location E:\Leemo
node smoke\smoke-cc-sdk.mjs --provider deepseek --check streaming
```

预期：打印 `PASS`，`streamEvents` ≥5。若 FAIL/ERROR：把完整输出原样记入 `smoke/results/` 对应 JSON（runner 已自动写），**不许改判定阈值来凑 PASS**——真实失败就是 Phase 0 要发现的东西。

- [ ] **Step 5: DeepSeek 五项全跑**

```powershell
Set-Location E:\Leemo
node smoke\smoke-cc-sdk.mjs --provider deepseek --check all
```

预期：五项逐个打印，末行 `DeepSeek: N/5 checks pass → smoke\results\deepseek-....json`。逐项耗时约 20s-3min，compaction 最慢，属正常。

- [ ] **Step 6: 提交**

```powershell
Set-Location E:\Leemo; git add smoke; git commit -m "feat(smoke): five core checks (streaming/tools/multiturn/subagent/compaction)"
```

**验收命令：** `node smoke\smoke-cc-sdk.mjs --provider deepseek --check all` 复跑；核对 results JSON 里 `details` 有真实证据（streamEvents 数值、answerContent 内容、pre_tokens 数值）且无明文 key（搜 `sk-`）。

---

### Task 4: 四项探测实现 + DeepSeek 跑通

**Files:**
- Create: `E:\Leemo\smoke\probes.mjs`
- Modify: `E:\Leemo\smoke\smoke-cc-sdk.mjs`（只改 PROBES 注册处与 import）

**Interfaces:**
- Consumes: `lib.mjs`/`checks.mjs` 同款约定；`.env` 的 `RELAY_*` 变量（P3，可空跳过）。
- Produces: `probes.mjs` 导出 `probeResume(provider)/probeAnySearch()/probeRelayAnthropic()/probeCanUseTool(provider)`，签名 `async => ({ probe, ok, details })`（`ok: true|false|null`，null=跳过）。

**禁改清单：** 不改 `lib.mjs`/`providers.mjs`/`checks.mjs`；runner 只允许加 import 和填 PROBES。

- [ ] **Step 1: 写 `smoke/probes.mjs`**

```js
// smoke/probes.mjs — Phase 0 四项探测（不卡 PASS 门，只记录事实供后续设计）
import fs from 'node:fs/promises';
import path from 'node:path';
import { query } from '@anthropic-ai/claude-agent-sdk';
import { buildEnv, newWorkspace } from './lib.mjs';

function baseOptions(provider, cwd, extra = {}) {
  return { cwd, env: buildEnv(provider), permissionMode: 'acceptEdits', settingSources: [], maxTurns: 6, ...extra };
}

// P1: SDK session resume 在第三方端点下的可靠性（06 号 §六 持久化方案的分叉依据）
export async function probeResume(provider) {
  const cwd = newWorkspace(`${provider.id}-resume`);
  let sessionId = null;
  const q1 = query({ prompt: '记住：我的幸运数字是 7。只回复"好的"。', options: baseOptions(provider, cwd, { maxTurns: 3 }) });
  for await (const m of q1) { if (m.type === 'system' && m.subtype === 'init') sessionId = m.session_id; }
  if (!sessionId) return { probe: 'resume', ok: false, details: { error: '未拿到 session_id' } };

  const q2 = query({
    prompt: '我的幸运数字是多少？直接说数字。',
    options: baseOptions(provider, cwd, { maxTurns: 3, resume: sessionId }),
  });
  let text = '';
  for await (const m of q2) { if (m.type === 'result' && m.subtype === 'success') text = m.result || ''; }
  return { probe: 'resume', ok: /7|七/.test(text), details: { sessionId, answer: text.slice(0, 80) } };
}

// P2: AnySearch 国内直连（06 号 §四 联网搜索默认源的可达性）
export async function probeAnySearch() {
  const targets = ['https://www.anysearch.com/', 'https://api.anysearch.com/'];
  const out = [];
  for (const url of targets) {
    const t0 = Date.now();
    try {
      const res = await fetch(url, { signal: AbortSignal.timeout(10000) });
      out.push({ url, status: res.status, ms: Date.now() - t0 });
    } catch (e) { out.push({ url, error: String(e).slice(0, 120), ms: Date.now() - t0 }); }
  }
  return { probe: 'anysearch', ok: out.some(r => typeof r.status === 'number'), details: out };
}

// P3: 用户中转站是否原生提供 Anthropic 协议（决定网关是否必需覆盖它）
export async function probeRelayAnthropic() {
  const base = process.env.RELAY_BASE_URL, key = process.env.RELAY_API_KEY;
  const model = process.env.RELAY_MODEL || 'claude-sonnet-4-6';
  if (!base || !key) return { probe: 'relay-anthropic', ok: null, details: 'RELAY_* 未配置，跳过' };
  const url = base.replace(/\/+$/, '') + '/v1/messages';
  try {
    const res = await fetch(url, {
      method: 'POST',
      headers: { 'content-type': 'application/json', 'x-api-key': key, authorization: `Bearer ${key}`, 'anthropic-version': '2023-06-01' },
      body: JSON.stringify({ model, max_tokens: 32, messages: [{ role: 'user', content: '回复 OK 两个字母即可' }] }),
      signal: AbortSignal.timeout(30000),
    });
    const body = (await res.text()).slice(0, 200);
    return { probe: 'relay-anthropic', ok: res.status === 200, details: { url, status: res.status, body } };
  } catch (e) { return { probe: 'relay-anthropic', ok: false, details: { url, error: String(e).slice(0, 200) } }; }
}

// P4: canUseTool 回调在第三方端点下的行为（审批条 UI 的机制基础）
// permissionMode 'default' 下 Write 需要审批 → 必然经过 canUseTool
export async function probeCanUseTool(provider) {
  const cwd = newWorkspace(`${provider.id}-canusetool`);
  const calls = [];
  const q = query({
    prompt: '在当前目录创建 probe.txt，内容为 hello。',
    options: baseOptions(provider, cwd, {
      permissionMode: 'default',
      canUseTool: async (toolName, input) => {
        calls.push({ toolName, inputKeys: Object.keys(input) });
        return { behavior: 'allow', updatedInput: input };
      },
    }),
  });
  let result = null;
  for await (const m of q) { if (m.type === 'result') result = m; }
  let fileOk = false;
  try { fileOk = (await fs.readFile(path.join(cwd, 'probe.txt'), 'utf8')).includes('hello'); } catch {}
  return { probe: 'canusetool', ok: calls.length > 0 && fileOk, details: { calls, fileOk, subtype: result?.subtype } };
}
```

- [ ] **Step 2: 在 runner 注册**

`smoke/smoke-cc-sdk.mjs` 加 import，把 `export const PROBES = {};` 替换为：

```js
import { probeResume, probeAnySearch, probeRelayAnthropic, probeCanUseTool } from './probes.mjs';

export const PROBES = {
  resume: probeResume,
  anysearch: probeAnySearch,
  'relay-anthropic': probeRelayAnthropic,
  canusetool: probeCanUseTool,
};
```

- [ ] **Step 3: 语法自检 + DeepSeek 探测跑**

```powershell
Set-Location E:\Leemo; node --check smoke\probes.mjs; node --check smoke\smoke-cc-sdk.mjs
node smoke\smoke-cc-sdk.mjs --provider deepseek --check none --probes
```

预期：四个探测逐个打印 `ok=true/false/null` 与 details；anysearch 可能 false（这正是探测目的，如实记录）；relay 未配 RELAY_* 时 `ok=null`。

- [ ] **Step 4: 提交**

```powershell
Set-Location E:\Leemo; git add smoke; git commit -m "feat(smoke): four probes (resume/anysearch/relay/canUseTool)"
```

**验收命令：** `node smoke\smoke-cc-sdk.mjs --provider deepseek --check none --probes` 复跑；核对 results JSON 中 probes 数组有 4 条且 details 是真实观测值。

---

### Task 5: 全矩阵执行 + Phase 0 报告 + PASS 判定

**Files:**
- Create: `E:\Leemo\docs\reports\phase0-report.md`
- Test: `smoke/results/` 下 3 个 provider 的 JSON（证据，gitignore 不入库；报告中粘贴关键数值）

**Interfaces:**
- Consumes: Task 3/4 的全部 checks/probes；`.env` 三家 key（用户提供）。
- Produces: `phase0-report.md` = 后续所有设计任务引用的事实基线；明确的 PASS/FAIL 结论。

**禁改清单：** 本任务**不改任何代码**——跑出来什么写什么；发现 bug 记录后回上一任务修（另起 commit），不在报告任务里顺手改。

- [ ] **Step 1: 三端点全矩阵执行（挨个跑，避免限流干扰）**

```powershell
Set-Location E:\Leemo
node smoke\smoke-cc-sdk.mjs --provider deepseek --check all --probes
node smoke\smoke-cc-sdk.mjs --provider glm --check all --probes
node smoke\smoke-cc-sdk.mjs --provider kimi --check all --probes
```

预期：每家产出一个 results JSON。任何 FAIL/ERROR 保留原样。GLM/Kimi 的 probes 中 anysearch/relay 与 provider 无关会重复跑，无害。

- [ ] **Step 2: 撰写 `docs/reports/phase0-report.md`**

按此模板填入真实数据（矩阵单元格填 PASS/FAIL/ERROR + 关键数值）：

```markdown
# Phase 0 可行性验证报告

> 日期：2026-07-__ ／ SDK：@anthropic-ai/claude-agent-sdk@0.3.210 ／ 执行机：Windows 10
> 证据文件：smoke/results/（本地保留，未入库）；复现命令见 docs/plans/2026-07-19-phase0.md Task 5

## 一、五项核心验证矩阵

| 验证项 | DeepSeek (模型) | GLM (模型) | Kimi (模型) |
|--------|----------------|-----------|------------|
| ① 流式 streamEvents | | | |
| ② 工具调用 toolsUsed | | | |
| ③ 多轮 finalText 含暗号 | | | |
| ④ 子 agent Task 工具 | | | |
| ⑤ 压缩 pre/post_tokens | | | |

注：⑤为 /compact 手动触发（摘要请求真实走了各 provider）；自动触发需 10 万级 tokens 灌注，未测，Phase 1 长会话观察。

## 二、四项探测结果

| 探测 | 结果 | 对后续设计的含义 |
|------|------|-----------------|
| P1 SDK resume | | 06 号 §六：可靠→持久化以 resume 为主；不可靠→Bridge 走历史重放降级 |
| P2 AnySearch 直连 | | 06 号 §四：可达→默认源成立；不可达→fallback 链首位换成用户 key 源 |
| P3 中转站原生 Anthropic | | 06 号 §三：是→网关对该中转站非必需；否→网关必须覆盖 |
| P4 canUseTool | | 06 号 §2.9 审批条机制基础 |

## 三、失败与异常记录

（每条：现象 → 原始错误摘录（脱敏）→ 初判原因 → 是否阻塞）

## 四、PASS 判定（06 号 §八：DeepSeek 满血制）

- DeepSeek 五项：__/5 → **PASS / FAIL**
- GLM 降级记录：__；Kimi 降级记录：__
- 结论：Phase 0 **PASS/FAIL**。（FAIL 时：候选顺序 GLM→Kimi 顶上重判，写明顶上者成绩）

## 五、给下一阶段的事实清单

（模型名实测值、各端点特殊行为、耗时量级、费用量级 usage 汇总——后续任务卡直接引用）
```

- [ ] **Step 3: 提交并推送**

```powershell
Set-Location E:\Leemo
git add docs\reports\phase0-report.md
git commit -m "docs: Phase 0 feasibility report with pass verdict"
git push
```

**验收命令（验收方）：**

```powershell
Set-Location E:\Leemo
node smoke\smoke-cc-sdk.mjs --provider deepseek --check all
Get-Content docs\reports\phase0-report.md
```

复跑 DeepSeek 五项，与报告矩阵一致（允许耗时/tokens 数值波动，PASS/FAIL 结论必须一致）；报告无占位符、无明文 key；§四有明确 PASS/FAIL 结论。**报告 PASS → Phase 0 收口，进入下一批（02 规格 v2.0 + 网关竖切）；FAIL → 按 §三记录开修复任务。**

---

## Self-Review 记录

1. **Spec 覆盖**：06 号 §八 Phase 0 全部要素落卡——5 核心（Task 3）、4 探测（Task 4）、DeepSeek 满血制判定+候选顶上（Task 5 报告 §四）、smoke-cc-sdk.mjs+报告产出（Task 2/5）、仓库先行（Task 1，含 GitHub 私有仓与文档拷入）。宪法 B4 模型别名映射在 `buildEnv` 全槽位覆盖。
2. **占位符扫描**：报告模板的空格是执行时填真实数据的表格，非计划占位；其余步骤代码/命令完整。
3. **类型一致性**：`resolveProvider` 返回 `{id,name,baseUrl,apiKey,model}`，`buildEnv/baseOptions` 只用这四个字段，Task 3/4 一致；CHECKS/PROBES 注册表签名与 runner 调用（`fn(provider)`）一致；probeAnySearch/probeRelayAnthropic 不用 provider 参数但签名兼容（多传无害）。
4. **已知风险点已内嵌**：`settingSources: []` 隔离本机配置；`env` 整体替换需 spread（lib.mjs 已做）；结果 JSON 脱敏（redact）；compaction 用 /compact 手动触发的局限已写进报告模板注释。

---

## 执行期修订记录

- 2026-07-20（Task 2 审查 Critical 修复）：计划中 runner 示例代码与全局约束"打印时脱敏"自相矛盾，按约束条款修正实际代码——console 输出全部经 redact()（先 redact 后 slice）；redact 阈值 >8 放宽为 ≥4 并注释理由；arg() 缺值时回退默认值；未知 check 提示列出可选项；loadEnv 支持引号包裹的值。计划正文代码块未逐一回改，以仓库实际代码为准。
- 2026-07-20（Task 3 审查 plan-mandated 修复）：五项验证判定加固——子 agent 夹具改无数字文件名（防 f3.txt 与 /3|三/ 假阳）；多轮/压缩断言改"短语+数字"双条件且压缩补 success 条件与 subtype 证据；工具验证 token 换不可猜代号 obsidian-7413；baseOptions 加 10 分钟 abort 超时（防挂死）；压缩项注释补失败归因指引（先验证 /compact 触发路径再归因 provider）。以仓库代码为准。
