# 核心界面视觉精修验证（2026-08-03）

## 1. 本轮结论

本轮完成的是 Leemo 核心界面的第一轮真实组件精修，不是整套场景视觉毕业。工作台、搭子态、模型设置和任务交互已经从扁平、偏黄、层级不清的状态收回为安静的冷中性工作界面；暖色仅保留给 momo、品牌和明确选中态。

## 2. 用户可见变化

- 工作台输入区保持最大宽度和左右留白，以轻阴影形成浮动层；窄窗发送或停止按钮仍固定在右侧。
- 工作台侧栏可收起、可从图标栏恢复；当前本子和当前对话使用清晰但克制的高亮层级。
- AskUser 待选项保持等宽；Approval 的说明与动作在窄窗分行，不再挤成一排。二者完成后都降为一行轻回执。
- 具体失败原因已经显示在时间线时，不再额外重复一条泛化失败提示。
- 模型设置只有配置正文滚动，底部操作固定；首次配置不再面对一列空白的“已接入”区域。

## 3. 当前源码打包态用户路径

使用临时 `win-unpacked` 验收当前源码，验收后不保留临时包：

- 七个真实页面：搭子态、工作台、技能、定时任务、成果、搜索、模型设置。
- 四个视口：`1440x900`、`1280x720`、`1024x768`、`720x640`。
- 所有页面横向溢出为 0；输入区位于视口内；控件裁切为 0；未出现装饰渐变。
- 浏览器完整任务再次完成 7 轮工具调用、受管浏览器连接、截图回传、人工接管、同任务恢复、最终动作确认和真实点击。
- 浏览器复验外部 API 调用为 0，模型费用为 0。

结构化事实：

- `docs/research/audit-shots/r11-visual-pass-facts.json`
- `docs/research/audit-shots/packaged-browser-journey-facts.json`

## 4. 自动验证

```powershell
npm test
npm run typecheck
git diff --check
node scripts/verify-visual-r11.mjs
node scripts/e2e-packaged-browser-journey.cjs
```

- 全量测试：`180` 个文件，`2474/2474` 通过。
- 三套 TypeScript typecheck：0 错。
- 视觉结构化门：6/6 通过。
- 浏览器打包态任务：全部判据通过。
- 当前源码临时包 `app.asar`：`97,675,270 B`，约 `93.15 MiB`。

本轮只生成临时解包应用，没有构建新的 NSIS；没有新增运行时依赖，也没有改变 bundled Skills。因此最终安装器、冷启动与安装文件数仍引用统一 RC 的证据，不在这里重复宣称。

## 5. 独立评审与未完成项

一次独立 reviewer 指出的窄窗按钮锚点、失败重复、设置双滚动、侧栏不可收起、Approval 挤压和已解决卡片过重均已在唯一修复轮关闭。

OpenRouter `openai/gpt-image-2` 的临时参考图请求被上游地区策略以 403 拒绝；密钥没有写入代码、文档、日志或安装包，也没有生成图片。当前仍需后续处理的视觉项包括：

- 对话历史按时间和本子提升扫描效率；
- “和 momo 认识一下”、关系个性化等新页面的专属视觉；
- 英语学习、论文学习等差异化场景的组件与进步反馈视觉。

这些项目不回滚本轮核心界面改进，也不阻塞继续补齐通用 Agent 能力。
