# Slice-2.5 消息展示 UX 补全 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 把 S2 的消息展示重构成成熟 Agent 的对话模型：严格时序自然叠加、过程原位折叠、页脚替「完成」卡、常驻计划卡、liveness+打断、智能滚动。

**Architecture:** 纯时序渲染（`TurnBlock` 单遍按 items 顺序出气泡/过程折叠/页脚）；reducer 停止丢弃 usage/error，新增 usage/error variant；每次 send 起新 runId 使回合可分；store 加 activeRunId+interrupt 驱动 liveness；智能滚动为渲染层 hook。

**Tech Stack:** React 18 + TS（tsconfig.renderer，strict+DOM+jsx）+ Tailwind（--leemo-* tokens）+ Zustand vanilla + Vitest（jsdom）+ @testing-library/react。

**权威 spec：** `docs/superpowers/specs/2026-07-22-slice2.5-message-ux-design.md`
**上游：** S2 骨架拍已完成（branch `s2-cards`，262 测试绿）。本片在其上重构。
**数据源契约：** `src/bridge/events.ts` LeemoEvent（含 usage.final/error/thinking.delta）+ `bridge:interrupt` 通道。只 `import type`，不改。

## Global Constraints

- **禁改**：`src/gateway/**`、`src/bridge/**`（含 contract.ts/events.ts 只 import type）、`tsconfig.vendor.json`、`smoke/**`、node 测试行为。
- **Phase-1 gate 不碰**：gate#1（conversations.ts 的 `client.subscribe` 结构不动、不加 unsubscribe——只改 runId 来源）；gate#2（fixture-client.ts 的 `invoke()` `default:return undefined` 不动——interrupt 作为新 case 插在 default 前）。
- **hexagonal 铁律**：`components/**` 不得 import `bridge/client`/`bridge/fixture-client`（`guard.test.ts` 守卫）。经 `useConversations` context hook 读 store 是合法路径；纯卡读 props。
- **TDD**：reducer/store/逻辑先写失败测试；纯视觉=用户目验（穿衣拍，不在本 plan）。
- **命名**：仅 Leemo/momo；用户可见名词只 本子/成果。
- **不回归**：S2 的 262 测试全绿；typecheck 三段 exit 0。
- 本 plan 只覆盖**骨架拍**（TDD 逻辑 + 朴素占位）。K3 穿衣是独立后续。
- **token 只显示数不显示费用**（gate#1 未修，不接 live cost）。

---

## 文件结构（本片新建/改/删）

**改：**
- `src/renderer/stores/message-model.ts` — 加 `usage`/`error` variant + reducer 两分支（停止丢弃）
- `src/renderer/stores/conversations.ts` — 每 send 起新 runId；加 `activeRunId` + `interrupt()`
- `src/renderer/components/timeline/Timeline.tsx` — 重构为按回合渲染 TurnBlock + 挂滚动容器
- `src/renderer/bridge/fixture-client.ts` — interrupt case（掐流+emit finished）；send 用传入 runId 不再硬编码
- `src/renderer/components/BuddyShell.tsx` — 输入框上方挂 PinnedPlan；InputBox 传 interrupt/activeRunId
- `src/renderer/components/InputBox.tsx` — 干活时发送键变停止键

**新建：**
- `src/renderer/components/timeline/TurnBlock.tsx` — 一个回合的严格时序渲染
- `src/renderer/components/timeline/ProcessFold.tsx` — 连续过程项折叠段
- `src/renderer/components/timeline/MessageFooter.tsx` — 复制+token+路径警示
- `src/renderer/components/timeline/LiveStatusBar.tsx` — 实时状态行+停止
- `src/renderer/components/PinnedPlan.tsx` — 输入框上方常驻计划卡
- `src/renderer/components/timeline/useScrollFollow.ts` — 智能滚动 hook
- `src/renderer/components/timeline/BackToBottom.tsx` — 回到底部悬浮按钮

**删：**
- `src/renderer/components/timeline/RunGroup.tsx`（被 TurnBlock/ProcessFold 取代）
- `src/renderer/components/timeline/ResultCard.tsx`（数据改由 MessageFooter 渲染）

---

## Task 1: reducer 补 usage/error variant + 停止丢弃

新增 `usage`/`error` 两个 TimelineItem variant，reducer 折叠它们（S2 曾在 default 丢弃）。result 项保留。

**Files:**
- Modify: `src/renderer/stores/message-model.ts`
- Test: `src/renderer/stores/message-model.test.ts`（追加 describe 块）

**Interfaces:**
- Consumes: 现有 `applyEvent(items, event, runId)`、`TimelineItem`、`UsageRecord`(from contract, import type)。
- Produces: `TimelineItem` 增 `{ kind:"usage"; id; runId; usage: UsageRecord }` 和 `{ kind:"error"; id; runId; message: string }`；reducer 处理 `usage.final`/`error`。

- [ ] **Step 1: Write the failing test**

追加到 `message-model.test.ts` 末尾：

```ts
describe("applyEvent — usage + error (slice 2.5)", () => {
  const RUN = "run-1";
  it("usage.final appends a usage item carrying the UsageRecord", () => {
    const m = applyEvent([], { type: "usage.final", usage: {
      providerId: "deepseek", modelId: "deepseek-chat", inputTokens: 2400, outputTokens: 600,
      cacheReadTokens: 0, cacheCreationTokens: 0, costSource: "unpriced", tokensEstimated: false,
    } }, RUN);
    expect(m).toHaveLength(1);
    expect(m[0]).toMatchObject({ kind: "usage", runId: RUN });
    if (m[0].kind === "usage") {
      expect(m[0].usage.inputTokens).toBe(2400);
      expect(m[0].usage.outputTokens).toBe(600);
    }
  });

  it("error appends an error item with the message", () => {
    const m = applyEvent([], { type: "error", message: "run failed" }, RUN);
    expect(m[0]).toMatchObject({ kind: "error", runId: RUN, message: "run failed" });
  });

  it("thinking.delta is still deferred (unchanged)", () => {
    const start: TimelineItem[] = [{ kind: "text", id: "m0", runId: RUN, role: "momo", text: "x", streaming: false }];
    expect(applyEvent(start, { type: "thinking.delta", text: "…" }, RUN)).toEqual(start);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/renderer/stores/message-model.test.ts`
Expected: FAIL（usage/error 落 default，不产生 item）。

- [ ] **Step 3: Write minimal implementation**

在 `message-model.ts` 的 `TimelineItem` union 末尾（compact 之后）加：

```ts
  | { kind: "usage"; id: string; runId: string; usage: UsageRecord }
  | { kind: "error"; id: string; runId: string; message: string };
```

改 import 行加 `UsageRecord`：

```ts
import type { LeemoEvent, PathAudit, UsageRecord } from "../../bridge/contract";
```

在 `applyEvent` switch 的 `default` 之前插入：

```ts
    case "usage.final":
      return [...items, { kind: "usage", id: `m${items.length}`, runId, usage: event.usage }];
    case "error":
      return [...items, { kind: "error", id: `m${items.length}`, runId, message: event.message }];
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run src/renderer/stores/message-model.test.ts`
Expected: PASS（S2 的 ~15 + 新 3）。

- [ ] **Step 5: Commit**

```bash
git add src/renderer/stores/message-model.ts src/renderer/stores/message-model.test.ts
git commit -m "feat(fe): reducer folds usage.final + error into timeline (s2.5 task1)"
```

---

## Task 2: store 每 send 起新 runId + activeRunId + interrupt

runId 从固定常量改为每回合自增；store 暴露 `activeRunId`（liveness）和 `interrupt()`。

**Files:**
- Modify: `src/renderer/stores/conversations.ts`
- Test: `src/renderer/stores/conversations.test.ts`

**Interfaces:**
- Consumes: `applyEvent`, `TimelineItem`, `RENDERER_RUN_ID_INITIAL`；`BridgeClient.invoke`。
- Produces: `ConversationsState` 增 `activeRunId: string | null` 和 `interrupt: () => Promise<void>`。`send` 每次生成新 runId（`run-1`, `run-2`…），用户消息与该回合响应共享它；`run.finished`（reducer 折进的 result 项到达时）清 activeRunId。

- [ ] **Step 1: Write the failing test**

追加到 `conversations.test.ts`：

```ts
  it("each send starts a new runId; user message and its run share it", async () => {
    vi.useFakeTimers();
    const client = new FixtureBridgeClient({ reply: "hi", chunkDelayMs: 5 });
    const store = createConversationsStore(client);
    await store.getState().send("first");
    const run1 = store.getState().messages.find((m) => m.kind === "text" && m.role === "user")?.runId;
    await vi.advanceTimersByTimeAsync(200);
    await store.getState().send("second");
    const userMsgs = store.getState().messages.filter((m) => m.kind === "text" && m.role === "user");
    expect(userMsgs).toHaveLength(2);
    expect(userMsgs[0].runId).not.toBe(userMsgs[1].runId);
    vi.useRealTimers();
  });

  it("activeRunId is set on send and cleared on run.finished", async () => {
    vi.useFakeTimers();
    const client = new FixtureBridgeClient({ reply: "hi", chunkDelayMs: 5 });
    const store = createConversationsStore(client);
    await store.getState().send("go");
    expect(store.getState().activeRunId).not.toBeNull();
    await vi.advanceTimersByTimeAsync(200);
    expect(store.getState().activeRunId).toBeNull();
    vi.useRealTimers();
  });

  it("interrupt invokes bridge:interrupt", async () => {
    const calls: string[] = [];
    const client = {
      invoke: async (ch: string) => { calls.push(ch); return undefined as never; },
      subscribe: () => () => {},
    };
    // @ts-expect-error minimal stub for the port
    const store = createConversationsStore(client);
    await store.getState().interrupt();
    expect(calls).toContain("bridge:interrupt");
  });
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/renderer/stores/conversations.test.ts`
Expected: FAIL（runId 固定、无 activeRunId/interrupt）。

- [ ] **Step 3: Write minimal implementation**

全文替换 `src/renderer/stores/conversations.ts`：

```ts
import { createStore, type StoreApi } from "zustand/vanilla";
import type { BridgeClient } from "../bridge/client";
import { applyEvent, type TimelineItem, RENDERER_RUN_ID_INITIAL } from "./message-model";

export interface ConversationsState {
  activeId: string;
  messages: TimelineItem[];
  activeRunId: string | null;
  send: (text: string) => Promise<void>;
  interrupt: () => Promise<void>;
}

export function createConversationsStore(client: BridgeClient): StoreApi<ConversationsState> {
  let runSeq = 0;
  let currentRunId = RENDERER_RUN_ID_INITIAL;

  const store = createStore<ConversationsState>((set, get) => ({
    activeId: "conv-1",
    messages: [],
    activeRunId: null,
    send: async (text: string) => {
      runSeq += 1;
      currentRunId = `run-${runSeq}`;
      const { messages, activeId } = get();
      const userMsg: TimelineItem = { kind: "text", id: `u${messages.length}`, runId: currentRunId, role: "user", text, streaming: false };
      set({ messages: [...messages, userMsg], activeRunId: currentRunId });
      await client.invoke("bridge:send", { conversationId: activeId, prompt: text });
    },
    interrupt: async () => {
      await client.invoke("bridge:interrupt", { conversationId: get().activeId });
    },
  }));

  client.subscribe("bridge:event", (event) => {
    store.setState((s) => {
      const messages = applyEvent(s.messages, event, currentRunId);
      const activeRunId = event.type === "run.finished" ? null : s.activeRunId;
      return { messages, activeRunId };
    });
  });

  return store;
}
```

> gate#1 不碰：`client.subscribe(...)` 仍 top-level、无 unsubscribe；只把 runId 从 const 换成工厂作用域可变，callback 读 currentRunId。
> 注：`bridge:interrupt` 的 request 类型是 `ConversationRef`（`{ conversationId }`），见 contract.ts:306。

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run src/renderer/stores/conversations.test.ts`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/renderer/stores/conversations.ts src/renderer/stores/conversations.test.ts
git commit -m "feat(fe): per-send runId + activeRunId + interrupt in store (s2.5 task2)"
```

---

## Task 3: fixture interrupt + demo turn 用当前 runId

fixture 支持 interrupt（掐待发定时器 + emit finished）。gate#2 不碰。

**Files:**
- Modify: `src/renderer/bridge/fixture-client.ts`
- Test: `src/renderer/bridge/fixture-client.test.ts`（追加 interrupt 测试）

**Interfaces:**
- Consumes: `BridgeInvokeMap`, `LeemoEvent`, `DEMO_TURN_EVENTS`。
- Produces: `invoke("bridge:interrupt")` 清所有待发 timeout + emit `run.finished{subtype:"interrupted", isError:false, finalText:"", pathAudit:{claimed:[]}}`。

- [ ] **Step 1: Write the failing test**

追加到 `fixture-client.test.ts`：

```ts
  it("interrupt cancels pending demo events and emits an interrupted run.finished", async () => {
    vi.useFakeTimers();
    const client = new FixtureBridgeClient({ chunkDelayMs: 5 });
    const events: LeemoEvent[] = [];
    client.subscribe("bridge:event", (e) => events.push(e));
    await client.invoke("bridge:send", { conversationId: "conv-1", prompt: "go" });
    await vi.advanceTimersByTimeAsync(15); // let a couple events fire
    const before = events.length;
    await client.invoke("bridge:interrupt", { conversationId: "conv-1" });
    await vi.advanceTimersByTimeAsync(500);
    expect(events.at(-1)).toMatchObject({ type: "run.finished", subtype: "interrupted" });
    // no full demo tail delivered after interrupt (fewer than the full ~17)
    expect(events.length).toBeLessThan(before + 17);
    vi.useRealTimers();
  });
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/renderer/bridge/fixture-client.test.ts`
Expected: FAIL（interrupt 落 default，无 finished、demo 全部照发）。

- [ ] **Step 3: Write minimal implementation**

在 `fixture-client.ts`：新增字段跟踪待发 timer；`scriptReply` 里把 `setTimeout` 句柄推入数组；加 `interrupt` 私有方法；`invoke` 加 case。

字段（class 顶部，与现有 private 并列）：

```ts
  private pending: ReturnType<typeof setTimeout>[] = [];
```

把 `scriptReply` 中每个 `setTimeout(...)` 改为 `this.pending.push(setTimeout(...))`（demo 循环那句：`this.pending.push(setTimeout(() => this.emit(ev), (t += step)))`；custom-reply 分支同理包裹每个）。

在 `invoke` 的 switch 里，`default` 之前加：

```ts
      case "bridge:interrupt":
        this.doInterrupt();
        return undefined as BridgeInvokeMap[K]["response"];
```

加私有方法：

```ts
  private doInterrupt(): void {
    for (const h of this.pending) clearTimeout(h);
    this.pending = [];
    this.emit({ type: "run.finished", subtype: "interrupted", isError: false, finalText: "", pathAudit: { claimed: [] } });
  }
```

> gate#2 不碰：`default: return undefined as ...` 分支原样；interrupt 是新 case 插在其前。

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run src/renderer/bridge/fixture-client.test.ts`
Expected: PASS（S2 的 3 + 新 1）。

- [ ] **Step 5: Commit**

```bash
git add src/renderer/bridge/fixture-client.ts src/renderer/bridge/fixture-client.test.ts
git commit -m "feat(fe): fixture interrupt cancels pending stream + emits interrupted (s2.5 task3)"
```

---

## Task 4: TurnBlock + ProcessFold + MessageFooter（时序重构核心）

替换 RunGroup 的 hoist 逻辑：TurnBlock 严格时序渲染一个回合；连续过程项就地聚成 ProcessFold（完成默认折叠）；result→MessageFooter。删 RunGroup + ResultCard。

**Files:**
- Create: `src/renderer/components/timeline/TurnBlock.tsx`, `ProcessFold.tsx`, `MessageFooter.tsx`
- Create: `src/renderer/components/timeline/turnblock.test.tsx`
- Delete: `src/renderer/components/timeline/RunGroup.tsx`, `ResultCard.tsx`, `timeline.test.tsx`(RunGroup 测试，被 turnblock.test.tsx 取代)

**Interfaces:**
- Consumes: `TimelineItem`（import type）；卡组件 TextBubble/ToolCard/PlanCard/ActivityCard/CompactDivider。
- Produces: `TurnBlock({ items, active }: { items: TimelineItem[]; active: boolean })`（一个回合的 items + 该回合是否进行中）；`ProcessFold({ items, defaultCollapsed }: { items: TimelineItem[]; defaultCollapsed: boolean })`；`MessageFooter({ result, usage }: { result: Extract<TimelineItem,{kind:"result"}>; usage?: Extract<TimelineItem,{kind:"usage"}> })`。

- [ ] **Step 1: Write the failing test**

`src/renderer/components/timeline/turnblock.test.tsx`：

```tsx
import { render, screen, fireEvent } from "@testing-library/react";
import { describe, it, expect } from "vitest";
import type { TimelineItem } from "../../stores/message-model";
import TurnBlock from "./TurnBlock";

const R = "run-1";
const user: TimelineItem = { kind: "text", id: "u", runId: R, role: "user", text: "整理笔记", streaming: false };
const open: TimelineItem = { kind: "text", id: "o", runId: R, role: "momo", text: "好，我先看看。", streaming: false };
const tool: TimelineItem = { kind: "tool", id: "t", runId: R, toolUseId: "t1", name: "Read", input: {}, status: "ok", summary: "38 页" };
const finalT: TimelineItem = { kind: "text", id: "f", runId: R, role: "momo", text: "草稿好了。", streaming: false };
const usage: TimelineItem = { kind: "usage", id: "us", runId: R, usage: { providerId: "p", modelId: "m", inputTokens: 2400, outputTokens: 600, cacheReadTokens: 0, cacheCreationTokens: 0, costSource: "unpriced", tokensEstimated: false } };
const result: TimelineItem = { kind: "result", id: "r", runId: R, isError: false, finalText: "草稿好了。", pathAudit: { claimed: [] } };

describe("TurnBlock strict-chronological rendering", () => {
  it("renders user message, then momo text, in document order (user is NOT hoisted below process)", () => {
    render(<TurnBlock items={[user, open, tool, finalT, usage, result]} active={false} />);
    const u = screen.getByText("整理笔记");
    const o = screen.getByText("好，我先看看。");
    // user appears before momo opening in DOM order
    expect(u.compareDocumentPosition(o) & Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy();
  });

  it("finished turn collapses the process fold by default; final text + footer stay visible", () => {
    render(<TurnBlock items={[user, open, tool, finalT, usage, result]} active={false} />);
    expect(screen.getByText("草稿好了。")).toBeInTheDocument();
    expect(screen.queryByText("Read")).not.toBeInTheDocument(); // process folded
    expect(screen.getByText(/复制/)).toBeInTheDocument(); // footer copy
    expect(screen.getByText(/2\.4k|2400/)).toBeInTheDocument(); // token stat
  });

  it("no 完成 card is rendered", () => {
    render(<TurnBlock items={[user, open, tool, finalT, usage, result]} active={false} />);
    expect(screen.queryByText(/^完成$/)).not.toBeInTheDocument();
  });

  it("active turn shows the process fold expanded", () => {
    const running: TimelineItem = { ...tool, status: "running", summary: undefined };
    render(<TurnBlock items={[user, open, running]} active={true} />);
    expect(screen.getByText("Read")).toBeInTheDocument();
  });

  it("clicking the fold bar reveals process cards on a finished turn", () => {
    render(<TurnBlock items={[user, open, tool, finalT, usage, result]} active={false} />);
    fireEvent.click(screen.getByRole("button", { name: /过程|查看|momo/ }));
    expect(screen.getByText("Read")).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/renderer/components/timeline/turnblock.test.tsx`
Expected: FAIL（组件不存在）。

- [ ] **Step 3: Write minimal implementation**

`MessageFooter.tsx`：

```tsx
import type { TimelineItem } from "../../stores/message-model";

function fmt(n: number): string {
  return n >= 1000 ? `${(n / 1000).toFixed(1)}k` : String(n);
}

export default function MessageFooter({
  result, usage,
}: { result: Extract<TimelineItem, { kind: "result" }>; usage?: Extract<TimelineItem, { kind: "usage" }> }) {
  const escaped = result.pathAudit.claimed.filter((c) => !c.withinCwd);
  return (
    <div className="mt-1 flex items-center gap-3 text-xs text-[var(--leemo-ink-3)]">
      <button type="button" className="inline-flex items-center gap-1 hover:text-[var(--leemo-ink-2)]"
        onClick={() => void navigator.clipboard?.writeText(result.finalText)}>
        <span aria-hidden>⧉</span> 复制
      </button>
      {usage && (
        <span className="tabular-nums">↑{fmt(usage.usage.inputTokens)} ↓{fmt(usage.usage.outputTokens)}</span>
      )}
      {escaped.length > 0 && (
        <span className="text-[var(--leemo-amber-ink,#8A6210)]">⚠ 声称写到工作区外：{escaped.map((c) => c.path).join("、")}</span>
      )}
    </div>
  );
}
```

`ProcessFold.tsx`（连续过程项的折叠段；带完成→折叠的 transition，沿用 S2 修复的 render-phase 模式）：

```tsx
import { useState } from "react";
import type { TimelineItem } from "../../stores/message-model";
import ToolCard from "./ToolCard";
import PlanCard from "./PlanCard";
import ActivityCard from "./ActivityCard";
import CompactDivider from "./CompactDivider";

function renderProcess(item: TimelineItem) {
  switch (item.kind) {
    case "tool": return <ToolCard key={item.id} item={item} />;
    case "plan": return <PlanCard key={item.id} item={item} />;
    case "activity": return <ActivityCard key={item.id} item={item} />;
    case "compact": return <CompactDivider key={item.id} item={item} />;
    default: return null;
  }
}

export default function ProcessFold({ items, defaultCollapsed }: { items: TimelineItem[]; defaultCollapsed: boolean }) {
  const [collapsed, setCollapsed] = useState(defaultCollapsed);
  const [prevDefault, setPrevDefault] = useState(defaultCollapsed);
  if (defaultCollapsed !== prevDefault) {
    setPrevDefault(defaultCollapsed);
    setCollapsed(defaultCollapsed);
  }
  if (items.length === 0) return null;
  return (
    <div className="my-1">
      <button type="button" onClick={() => setCollapsed((v) => !v)}
        className="flex items-center gap-2 text-xs text-[var(--leemo-ink-3)] hover:text-[var(--leemo-ink-2)]">
        <span aria-hidden>{collapsed ? "▸" : "▾"}</span>
        <span>{collapsed ? "查看过程" : "收起过程"} · momo 干了 {items.length} 步</span>
      </button>
      {!collapsed && <div className="mt-1 space-y-1.5">{items.map(renderProcess)}</div>}
    </div>
  );
}
```

`TurnBlock.tsx`（严格时序单遍：把连续过程项聚成 ProcessFold，其余原位）：

```tsx
import type { TimelineItem } from "../../stores/message-model";
import TextBubble from "./TextBubble";
import ProcessFold from "./ProcessFold";
import MessageFooter from "./MessageFooter";

const isProcess = (i: TimelineItem) =>
  i.kind === "tool" || i.kind === "plan" || i.kind === "activity" || i.kind === "compact";

export default function TurnBlock({ items, active }: { items: TimelineItem[]; active: boolean }) {
  const usage = items.find((i) => i.kind === "usage") as Extract<TimelineItem, { kind: "usage" }> | undefined;
  const nodes: React.ReactNode[] = [];
  let processBuf: TimelineItem[] = [];
  const flush = (key: string) => {
    if (processBuf.length > 0) {
      nodes.push(<ProcessFold key={`pf-${key}`} items={processBuf} defaultCollapsed={!active} />);
      processBuf = [];
    }
  };
  items.forEach((it, idx) => {
    if (isProcess(it)) { processBuf.push(it); return; }
    flush(String(idx));
    if (it.kind === "text") nodes.push(<TextBubble key={it.id} item={it} />);
    else if (it.kind === "result") nodes.push(<MessageFooter key={it.id} result={it} usage={usage} />);
    else if (it.kind === "error") nodes.push(
      <div key={it.id} className="text-xs text-[var(--leemo-danger,#B0776E)]">⚠ {it.message}</div>
    );
    // usage 项本身不单独渲染（由 footer 消费）
  });
  flush("tail");
  return <div className="space-y-2">{nodes}</div>;
}
```

删 `RunGroup.tsx`、`ResultCard.tsx`、`timeline.test.tsx`（`git rm`）。

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run src/renderer/components/timeline/turnblock.test.tsx` — PASS（5 tests）。
Run: `npx vitest run src/renderer/components/guard.test.ts` — PASS（铁律）。

- [ ] **Step 5: Commit**

```bash
git add src/renderer/components/timeline/TurnBlock.tsx src/renderer/components/timeline/ProcessFold.tsx src/renderer/components/timeline/MessageFooter.tsx src/renderer/components/timeline/turnblock.test.tsx
git rm src/renderer/components/timeline/RunGroup.tsx src/renderer/components/timeline/ResultCard.tsx src/renderer/components/timeline/timeline.test.tsx
git commit -m "feat(fe): strict-chronological TurnBlock + ProcessFold + MessageFooter, retire RunGroup/ResultCard/完成 card (s2.5 task4)"
```

---

## Task 5: Timeline 按回合渲染 TurnBlock

Timeline 用 groupByRun 分回合，每回合一个 TurnBlock；active 回合 = activeRunId 匹配。

**Files:**
- Modify: `src/renderer/components/timeline/Timeline.tsx`
- Test: `src/renderer/components/timeline/timeline-groups.test.tsx`（新）

**Interfaces:**
- Consumes: `useConversations`（读 messages + activeRunId）；`TurnBlock`；S2 已有的 `groupByRun`（保留）。
- Produces: `Timeline`（default export，无 props）。每组 items 交给 TurnBlock，`active = group.runId === activeRunId`。

- [ ] **Step 1: Write the failing test**

`src/renderer/components/timeline/timeline-groups.test.tsx`：

```tsx
import { render, screen } from "@testing-library/react";
import { describe, it, expect } from "vitest";
import { BridgeProvider } from "../../bridge/context";
import Timeline from "./Timeline";
import { FixtureBridgeClient } from "../../bridge/fixture-client";

// Render Timeline inside the provider with a fixture client that has emitted nothing yet.
describe("Timeline groups items into per-run TurnBlocks", () => {
  it("renders nothing when there are no messages", () => {
    const { container } = render(
      <BridgeProvider client={new FixtureBridgeClient()}>
        <Timeline />
      </BridgeProvider>,
    );
    expect(container.querySelector("*")).toBeTruthy(); // provider renders, timeline empty
    expect(screen.queryByText(/复制/)).not.toBeInTheDocument();
  });
});
```

> 注：多回合分组的核心逻辑（groupByRun）已在 S2 测过；本 Task 的测试只锁 Timeline 装配（空态不崩、经 provider 读 store）。完整多回合目验由验收②覆盖。若 groupByRun 需从 RunGroup 迁出，一并移入 Timeline.tsx 或 utils。

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/renderer/components/timeline/timeline-groups.test.tsx`
Expected: FAIL（Timeline 仍引用已删的 RunGroup → import error）。

- [ ] **Step 3: Write minimal implementation**

全文替换 `src/renderer/components/timeline/Timeline.tsx`：

```tsx
import { useConversations } from "../../bridge/context";
import type { TimelineItem } from "../../stores/message-model";
import TurnBlock from "./TurnBlock";

/** Groups items by runId (compact items, which have no runId, attach to the
 *  current group) and renders one TurnBlock per run. */
function groupByRun(items: TimelineItem[]): { runId: string; items: TimelineItem[] }[] {
  const groups: { runId: string; items: TimelineItem[] }[] = [];
  for (const item of items) {
    const run = item.kind === "compact" ? groups[groups.length - 1]?.runId : item.runId;
    const last = groups[groups.length - 1];
    if (last && last.runId === run) last.items.push(item);
    else if (run !== undefined) groups.push({ runId: run, items: [item] });
    else if (last) last.items.push(item); // leading compact w/o group: attach to last (none → dropped)
  }
  return groups;
}

export default function Timeline() {
  const messages = useConversations((s) => s.messages);
  const activeRunId = useConversations((s) => s.activeRunId);
  if (messages.length === 0) return null;
  const groups = groupByRun(messages);
  return (
    <div className="mx-auto w-full max-w-[720px] space-y-6 py-4">
      {groups.map((g) => (
        <TurnBlock key={g.runId} items={g.items} active={g.runId === activeRunId} />
      ))}
    </div>
  );
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run src/renderer/components/timeline/timeline-groups.test.tsx` — PASS。
Run: `npx vitest run src/renderer/app/App.test.tsx` — PASS（BuddyShell 仍渲染 greeting）。

- [ ] **Step 5: Commit**

```bash
git add src/renderer/components/timeline/Timeline.tsx src/renderer/components/timeline/timeline-groups.test.tsx
git commit -m "feat(fe): Timeline renders per-run TurnBlocks with active flag (s2.5 task5)"
```

---

## Task 6: PinnedPlan 常驻计划卡（输入框上方）

读最新 plan 项的 todo+进度，钉在输入框上方，默认折叠，可展开。

**Files:**
- Create: `src/renderer/components/PinnedPlan.tsx`
- Create: `src/renderer/components/PinnedPlan.test.tsx`
- Modify: `src/renderer/components/BuddyShell.tsx`（输入框上方挂 PinnedPlan）

**Interfaces:**
- Consumes: `useConversations`（读 messages，选出最新 plan 项）；`TimelineItem`。
- Produces: `PinnedPlan`（default export，无 props，读 store）。无 plan → 不渲染（null）。有 → 折叠条（当前任务 · N/M）+ 展开列 todo。

- [ ] **Step 1: Write the failing test**

`src/renderer/components/PinnedPlan.test.tsx`：

```tsx
import { render, screen, fireEvent } from "@testing-library/react";
import { describe, it, expect } from "vitest";
import { BridgeProvider } from "../bridge/context";
import PinnedPlan from "./PinnedPlan";
import type { BridgeClient } from "../bridge/client";
import type { TimelineItem } from "../stores/message-model";

// A stub client whose store we seed by emitting a TodoWrite tool.started.
function clientWithPlan(): BridgeClient {
  let cb: ((e: unknown) => void) | null = null;
  return {
    invoke: async () => undefined as never,
    subscribe: (_ch, fn) => { cb = fn as (e: unknown) => void; queueMicrotask(() => cb?.({
      type: "tool.started", toolUseId: "p1", name: "TodoWrite", subagent: false,
      input: { todos: [
        { content: "提取 pptx", status: "completed" },
        { content: "生成草稿", status: "in_progress" },
      ] },
    })); return () => {}; },
  } as BridgeClient;
}

describe("PinnedPlan", () => {
  it("renders nothing when there is no plan", () => {
    const noPlan = { invoke: async () => undefined as never, subscribe: () => () => {} } as BridgeClient;
    const { container } = render(<BridgeProvider client={noPlan}><PinnedPlan /></BridgeProvider>);
    expect(container.textContent).not.toMatch(/当前任务/);
  });

  it("shows the latest plan's progress and expands to list todos", async () => {
    render(<BridgeProvider client={clientWithPlan()}><PinnedPlan /></BridgeProvider>);
    expect(await screen.findByText(/当前任务/)).toBeInTheDocument();
    expect(screen.getByText(/1\s*\/\s*2/)).toBeInTheDocument();
    fireEvent.click(screen.getByRole("button"));
    expect(screen.getByText("生成草稿")).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/renderer/components/PinnedPlan.test.tsx`
Expected: FAIL（组件不存在）。

- [ ] **Step 3: Write minimal implementation**

`src/renderer/components/PinnedPlan.tsx`：

```tsx
import { useState } from "react";
import { useConversations } from "../bridge/context";
import type { TimelineItem } from "../stores/message-model";

export default function PinnedPlan() {
  const [open, setOpen] = useState(false);
  const plan = useConversations((s) => {
    for (let i = s.messages.length - 1; i >= 0; i--) {
      const it = s.messages[i];
      if (it.kind === "plan") return it as Extract<TimelineItem, { kind: "plan" }>;
    }
    return undefined;
  });
  if (!plan) return null;
  const done = plan.todos.filter((t) => t.status === "done").length;
  return (
    <div className="mx-auto w-full max-w-[640px]">
      <div className="rounded-[12px] border border-[var(--leemo-line)] bg-white/70">
        <button type="button" onClick={() => setOpen((v) => !v)}
          className="flex w-full items-center gap-2 px-3 py-1.5 text-xs text-[var(--leemo-ink-2)]">
          <span aria-hidden>{open ? "▾" : "▸"}</span>
          <span className="font-medium">当前任务</span>
          <span className="ml-auto tabular-nums text-[var(--leemo-ink-3)]">{done} / {plan.todos.length}</span>
        </button>
        {open && (
          <ol className="border-t border-[var(--leemo-line)] px-3 py-1.5 space-y-0.5 text-[13px]">
            {plan.todos.map((t, i) => (
              <li key={i} className="text-[var(--leemo-ink)]">
                <span className="mr-1.5 text-xs" aria-hidden>{t.status === "done" ? "✓" : t.status === "active" ? "▸" : "○"}</span>
                {t.text}
              </li>
            ))}
          </ol>
        )}
      </div>
    </div>
  );
}
```

改 `BuddyShell.tsx`：import PinnedPlan，在 `<ChipRow>` 之上（输入框区顶部）插入 `<PinnedPlan />`。具体：把 `<div className="mt-auto pb-8">` 块改为：

```tsx
        <div className="mt-auto pb-8">
          <PinnedPlan />
          <ChipRow onPick={setDraft} />
          <InputBox value={draft} onChange={setDraft} onSend={(t) => void send(t)} />
          <PinFootnote text="周六 23:59 ·《高等数学》第五章作业截止 —— 到时候我提醒你" />
        </div>
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run src/renderer/components/PinnedPlan.test.tsx` — PASS。
Run: `npx vitest run src/renderer/components/guard.test.ts` — PASS（PinnedPlan 经 context hook，不 import 端口）。

- [ ] **Step 5: Commit**

```bash
git add src/renderer/components/PinnedPlan.tsx src/renderer/components/PinnedPlan.test.tsx src/renderer/components/BuddyShell.tsx
git commit -m "feat(fe): pinned plan card above input (latest todos + progress) (s2.5 task6)"
```

---

## Task 7: liveness 状态行 + InputBox 停止键

干活时 InputBox 发送键变停止键（调 interrupt）；LiveStatusBar 显示进行中工具。

**Files:**
- Create: `src/renderer/components/timeline/LiveStatusBar.tsx`
- Modify: `src/renderer/components/InputBox.tsx`, `src/renderer/components/BuddyShell.tsx`
- Test: `src/renderer/components/InputBox.test.tsx`（追加停止态断言）, `src/renderer/components/timeline/LiveStatusBar.test.tsx`（新）

**Interfaces:**
- Consumes: `useConversations`（activeRunId、interrupt、messages）。
- Produces: `InputBox` 增 props `{ busy?: boolean; onStop?: () => void }`——busy 时渲染停止按钮（aria-label "停止"）调 onStop，否则发送按钮（现有）。`LiveStatusBar({ toolName }: { toolName?: string })` 纯展示（BuddyShell 从 store 算最新进行中工具名传入，或组件自读——本片让 BuddyShell 读 store 传 props 保持纯展示）。

- [ ] **Step 1: Write the failing test**

追加到 `InputBox.test.tsx`：

```tsx
  it("shows a stop button when busy and calls onStop", () => {
    const onStop = vi.fn();
    render(<InputBox value="" onChange={() => {}} onSend={() => {}} busy onStop={onStop} />);
    const stop = screen.getByRole("button", { name: "停止" });
    fireEvent.click(stop);
    expect(onStop).toHaveBeenCalled();
  });

  it("shows the send button when not busy", () => {
    render(<InputBox value="hi" onChange={() => {}} onSend={() => {}} />);
    expect(screen.getByRole("button", { name: "发送" })).toBeInTheDocument();
  });
```

（确保文件顶部 import 有 `vi`, `fireEvent`, `render`, `screen`。）

`src/renderer/components/timeline/LiveStatusBar.test.tsx`：

```tsx
import { render, screen } from "@testing-library/react";
import { describe, it, expect } from "vitest";
import LiveStatusBar from "./LiveStatusBar";

describe("LiveStatusBar", () => {
  it("renders the running tool name", () => {
    render(<LiveStatusBar toolName="Read" />);
    expect(screen.getByText(/Read/)).toBeInTheDocument();
  });
  it("renders nothing without a tool name", () => {
    const { container } = render(<LiveStatusBar />);
    expect(container.textContent).toBe("");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/renderer/components/InputBox.test.tsx src/renderer/components/timeline/LiveStatusBar.test.tsx`
Expected: FAIL（busy/onStop props 不存在；LiveStatusBar 不存在）。

- [ ] **Step 3: Write minimal implementation**

改 `InputBox.tsx` 签名 + 按钮：

```tsx
export default function InputBox({
  value, onChange, onSend, busy = false, onStop,
}: { value: string; onChange: (v: string) => void; onSend: (v: string) => void; busy?: boolean; onStop?: () => void }) {
```

把发送 `<button>` 换成条件渲染：

```tsx
      {busy ? (
        <button aria-label="停止" onClick={() => onStop?.()}
          className="grid h-9 w-9 shrink-0 place-items-center rounded-full bg-[var(--leemo-ink)] text-white shadow-sm transition-colors hover:bg-black">
          <span className="block h-3 w-3 rounded-[2px] bg-white" aria-hidden />
        </button>
      ) : (
        <button aria-label="发送" onClick={submit}
          className="grid h-9 w-9 shrink-0 place-items-center rounded-full bg-[var(--leemo-amber)] text-white shadow-sm transition-colors hover:bg-[var(--leemo-amber-strong)]">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2} strokeLinecap="round" strokeLinejoin="round" className="h-[15px] w-[15px]" aria-hidden>
            <path d="M12 19V5" /><path d="m5 12 7-7 7 7" />
          </svg>
        </button>
      )}
```

`LiveStatusBar.tsx`：

```tsx
export default function LiveStatusBar({ toolName }: { toolName?: string }) {
  if (!toolName) return null;
  return (
    <span className="inline-flex items-center gap-1.5 text-xs text-[var(--leemo-ink-3)]">
      <span className="inline-block h-3 w-3 animate-spin rounded-full border border-current border-t-transparent" aria-hidden />
      正在 {toolName}…
    </span>
  );
}
```

改 `BuddyShell.tsx`：读 activeRunId/interrupt/最新进行中工具，传给 InputBox + 渲染 LiveStatusBar。加：

```tsx
  const activeRunId = useConversations((s) => s.activeRunId);
  const interrupt = useConversations((s) => s.interrupt);
  const runningTool = useConversations((s) => {
    if (!s.activeRunId) return undefined;
    for (let i = s.messages.length - 1; i >= 0; i--) {
      const it = s.messages[i];
      if (it.kind === "tool" && it.status === "running") return it.name;
    }
    return undefined;
  });
```

`<InputBox ... />` 改为：

```tsx
          <InputBox value={draft} onChange={setDraft} onSend={(t) => void send(t)}
            busy={activeRunId !== null} onStop={() => void interrupt()} />
```

在 PinnedPlan 与 InputBox 之间（或 InputBox 上方）放 `{runningTool && <div className="mx-auto max-w-[640px] px-1 pb-1"><LiveStatusBar toolName={runningTool} /></div>}`。

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run src/renderer/components/InputBox.test.tsx src/renderer/components/timeline/LiveStatusBar.test.tsx` — PASS。
Run: `npx vitest run src/renderer/components/guard.test.ts` — PASS。

- [ ] **Step 5: Commit**

```bash
git add src/renderer/components/InputBox.tsx src/renderer/components/InputBox.test.tsx src/renderer/components/timeline/LiveStatusBar.tsx src/renderer/components/timeline/LiveStatusBar.test.tsx src/renderer/components/BuddyShell.tsx
git commit -m "feat(fe): liveness status bar + stop button while a run is active (s2.5 task7)"
```

---

## Task 8: 智能滚动（跟随 / 上划暂停 / 回到底部按钮）

滚动容器：贴底自动跟随；用户上划中断跟随；回到底部悬浮按钮恢复跟随。

**Files:**
- Create: `src/renderer/components/timeline/useScrollFollow.ts`
- Create: `src/renderer/components/timeline/BackToBottom.tsx`
- Create: `src/renderer/components/timeline/useScrollFollow.test.tsx`
- Modify: `src/renderer/components/timeline/Timeline.tsx`（用 hook 包滚动容器 + 挂按钮）

**Interfaces:**
- Consumes: `messages`（变化触发跟随）。
- Produces: `useScrollFollow(dep: unknown): { containerRef, atBottom, scrollToBottom }`——`dep` 变化（新消息）时若 atBottom 则滚到底；监听 scroll 更新 atBottom（上划→false）。`BackToBottom({ show, onClick })` 悬浮按钮。

- [ ] **Step 1: Write the failing test**

`src/renderer/components/timeline/useScrollFollow.test.tsx`：

```tsx
import { render, screen, fireEvent } from "@testing-library/react";
import { describe, it, expect, vi } from "vitest";
import BackToBottom from "./BackToBottom";

describe("BackToBottom", () => {
  it("is hidden when show is false", () => {
    render(<BackToBottom show={false} onClick={() => {}} />);
    expect(screen.queryByRole("button", { name: /回到底部/ })).not.toBeInTheDocument();
  });
  it("shows and fires onClick", () => {
    const onClick = vi.fn();
    render(<BackToBottom show onClick={onClick} />);
    fireEvent.click(screen.getByRole("button", { name: /回到底部/ }));
    expect(onClick).toHaveBeenCalled();
  });
});
```

> 注：jsdom 不实现真实布局/滚动度量（scrollHeight/clientHeight 恒 0），故 useScrollFollow 的滚动数值逻辑无法在 jsdom 可靠单测——本 Task 单测只锁 BackToBottom 的显隐+回调（可测的纯组件），滚动跟随/上划暂停的行为由**验收②目验**覆盖。hook 实现保持精简、行为在浏览器验证。

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/renderer/components/timeline/useScrollFollow.test.tsx`
Expected: FAIL（BackToBottom 不存在）。

- [ ] **Step 3: Write minimal implementation**

`useScrollFollow.ts`：

```ts
import { useEffect, useRef, useState, useCallback } from "react";

/** Smart scroll: follow the streaming edge while pinned to the bottom; a manual
 *  scroll-up detaches follow (user reads history quietly); scrollToBottom
 *  re-attaches. `dep` changing (new messages) triggers a follow scroll when
 *  still at bottom. */
export function useScrollFollow(dep: unknown) {
  const containerRef = useRef<HTMLDivElement | null>(null);
  const [atBottom, setAtBottom] = useState(true);

  const scrollToBottom = useCallback(() => {
    const el = containerRef.current;
    if (el) el.scrollTop = el.scrollHeight;
    setAtBottom(true);
  }, []);

  const onScroll = useCallback(() => {
    const el = containerRef.current;
    if (!el) return;
    const near = el.scrollHeight - el.scrollTop - el.clientHeight < 40;
    setAtBottom(near);
  }, []);

  useEffect(() => {
    if (atBottom) {
      const el = containerRef.current;
      if (el) el.scrollTop = el.scrollHeight;
    }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [dep]);

  return { containerRef, atBottom, scrollToBottom, onScroll };
}
```

`BackToBottom.tsx`：

```tsx
export default function BackToBottom({ show, onClick }: { show: boolean; onClick: () => void }) {
  if (!show) return null;
  return (
    <button type="button" aria-label="回到底部" onClick={onClick}
      className="absolute bottom-4 right-4 z-20 grid h-9 w-9 place-items-center rounded-full border border-[var(--leemo-line)] bg-white text-[var(--leemo-ink-2)] shadow-[0_2px_10px_-2px_rgba(0,0,0,.18)] transition-all hover:text-[var(--leemo-ink)] hover:shadow-[0_4px_14px_-3px_rgba(0,0,0,.24)] animate-[fadeIn_.2s_ease]">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2} strokeLinecap="round" strokeLinejoin="round" className="h-[15px] w-[15px]" aria-hidden>
        <path d="M12 5v14" /><path d="m5 12 7 7 7-7" />
      </svg>
    </button>
  );
}
```

改 `Timeline.tsx`：用 hook 包滚动容器（`ref={containerRef}` + `onScroll` + `relative` + 内容 + `<BackToBottom show={!atBottom} onClick={scrollToBottom} />`）。dep = messages 长度或引用。容器需 `overflow-y-auto` + 限高（`h-full` 或 flex-1，由 BuddyShell 布局给）。改为：

```tsx
import { useConversations } from "../../bridge/context";
import type { TimelineItem } from "../../stores/message-model";
import TurnBlock from "./TurnBlock";
import { useScrollFollow } from "./useScrollFollow";
import BackToBottom from "./BackToBottom";

// groupByRun unchanged (from Task 5) ...

export default function Timeline() {
  const messages = useConversations((s) => s.messages);
  const activeRunId = useConversations((s) => s.activeRunId);
  const { containerRef, atBottom, scrollToBottom, onScroll } = useScrollFollow(messages);
  if (messages.length === 0) return null;
  const groups = groupByRun(messages);
  return (
    <div className="relative flex-1 overflow-hidden">
      <div ref={containerRef} onScroll={onScroll} className="h-full overflow-y-auto">
        <div className="mx-auto w-full max-w-[720px] space-y-6 py-4">
          {groups.map((g) => (
            <TurnBlock key={g.runId} items={g.items} active={g.runId === activeRunId} />
          ))}
        </div>
      </div>
      <BackToBottom show={!atBottom} onClick={scrollToBottom} />
    </div>
  );
}
```

> 若 `fadeIn` keyframe 未定义，加到 `index.css` 或 tokens.css：`@keyframes fadeIn{from{opacity:0}to{opacity:1}}`（骨架拍可省，不影响测试）。

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run src/renderer/components/timeline/useScrollFollow.test.tsx` — PASS。
Run: `npx vitest run src/renderer/components/guard.test.ts` — PASS。

- [ ] **Step 5: Commit**

```bash
git add src/renderer/components/timeline/useScrollFollow.ts src/renderer/components/timeline/BackToBottom.tsx src/renderer/components/timeline/useScrollFollow.test.tsx src/renderer/components/timeline/Timeline.tsx
git commit -m "feat(fe): smart scroll — follow edge, pause on scroll-up, back-to-bottom button (s2.5 task8)"
```

---

## Task 9: 全量验收

**Files:** 无（验证）。

- [ ] **Step 1: typecheck 三段** — `npm run typecheck` → 三段 exit 0。
- [ ] **Step 2: 全量测试** — `npm test` → 全绿，含 S2 的 262 基线不回归（本片净增测试；删了 timeline.test.tsx 的 3 个 RunGroup 测试，新增若干）。
- [ ] **Step 3: guard 铁律** — `npx vitest run src/renderer/components/guard.test.ts` → PASS（timeline/** + PinnedPlan 无端口 import）。
- [ ] **Step 4: 目验准备（验收②）** — `npm run dev`，人工目验：多回合自然时序叠加（用户消息不置顶成块）· 过程原位折叠（完成默认折叠/进行中展开）· 页脚复制+token 替「完成」· 输入框上方常驻计划卡 · 干活时停止键+liveness · 滚动跟随/上划暂停/回到底部按钮。
- [ ] **Step 5: Commit（若有修复）** — `git commit -m "chore(fe): slice-2.5 acceptance green (s2.5 task9)"`

---

## Self-Review

**Spec coverage：**
- §1 严格时序自然叠加 → Task 4（TurnBlock 单遍）+ Task 5（Timeline 分回合）✓
- §2 每 send 新 runId → Task 2 ✓
- §3 reducer 补 usage/error → Task 1 ✓
- §4 组件（TurnBlock/ProcessFold/MessageFooter/LiveStatusBar/PinnedPlan/滚动）→ Task 4,6,7,8 ✓；删 RunGroup/ResultCard → Task 4 ✓
- §5 liveness+打断 → Task 2（store interrupt/activeRunId）+ Task 3（fixture interrupt）+ Task 7（UI）✓
- §6 智能滚动（跟随/上划暂停/回到底部）→ Task 8 ✓
- §7 边界（产物卡留 S4、token 只数不费用）→ 未建产物卡；MessageFooter 只 token 数 ✓
- §8 gate#1/#2 不碰 → Task 2（subscribe 结构不动）、Task 3（invoke default 不动）显式声明 ✓；铁律 Task 9 复验

**Placeholder scan：** 无 TBD；每 code step 有完整代码。✓

**Type consistency：** `TimelineItem` 新 variant usage/error 在 Task 1 定义、Task 4（footer/error 渲染）消费；`activeRunId`/`interrupt` Task 2 定义、Task 5/7 消费；`TurnBlock({items,active})`、`ProcessFold({items,defaultCollapsed})`、`MessageFooter({result,usage})`、`InputBox({...busy,onStop})` 签名一致。✓

**已知取舍（非缺陷）：** jsdom 无布局度量 → useScrollFollow 滚动数值行为不单测，只测 BackToBottom 显隐 + 由目验②覆盖（Task 8 注明）。Timeline 多回合分组核心（groupByRun）S2 已测，Task 5 只锁装配。
