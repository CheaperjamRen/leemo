== RED-C (review-round fixes, tests written, impl NOT yet changed) ==
Focused: pitfall-02 + pitfall-09
      Tests  7 failed | 9 passed (16)

Failing (genuine RED for the review fixes):
  [Important] pitfall-02: isServerTool predicate single-source
    - isServerTool not exported from normalize → import undefined
  [Important] pitfall-02: divergent shape (versioned non-custom type WITH input_schema) stripped consistently
    - old vendor predicate `&& !input_schema` kept it; facade did not return stripped list
    - { result, stripped } destructure → anthropicToOpenAI still returned bare body
  [Important] pitfall-02: facade exposes stripped list (G3 reachable w/o vendor)
  [Important] pitfall-02: facade stripped empty when only client tools
  [Minor] pitfall-09: end-to-end reasoning gate now destructures { result } → RED until signature change
    TypeError: Cannot read properties of undefined (reading 'reasoning')

Note: pitfall-09 literal-budget assertions (4000/12000/24000/40000/60000/16000) PASS already
(impl constants correct); they now pin NewMax literals instead of self-referencing the imported map.
