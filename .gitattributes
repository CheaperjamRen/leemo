# This offline bundle is verified against a byte-level manifest. Preserve the
# committed bytes on every platform instead of applying Windows CRLF conversion.
bundled-skills/superpowers/release/** text eol=lf
